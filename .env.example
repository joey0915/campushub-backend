# CampusHub backend — environment template.
# Copy to `.env` and fill in real values. NEVER commit `.env`.

# Server
NODE_ENV=development
PORT=3000

# API
API_VERSION=v1

# MongoDB (Mongoose connection string)
MONGODB_URI=mongodb://127.0.0.1:27017/campushub
